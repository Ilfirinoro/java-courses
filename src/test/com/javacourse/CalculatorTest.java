/*
package com.javacourse;

import com.javacourse.calc.Calculator;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CalculatorTest {

    @Test
    public void add() {
        Calculator calculator = new Calculator();
        calculator.add(3, 5);
        assertEquals(
                "Некорректнй результат выполнения метода add.",
                8,
                calculator.getResult()
        );
    }

    @Test
    public void clearResult() {
        Calculator calculator = new Calculator();
        calculator.add(3, 5);
        calculator.clearResult();
        assertEquals(
                "Некорректнй результат выполнения метода clearResult.",
                0,
                calculator.getResult()
        );
    }
}*/
